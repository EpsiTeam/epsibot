/* eslint-disable no-irregular-whitespace */
export const text = `__En n'étant pas admin:__

- Voir toutes les commandes customs: \`/command list\`
Les commandes customs permettent de créer des réponses personnalisées à certains messages. Ces réponses peuvent s'afficher dans un embed ou dans un message normal

- Faire une partie de morpion: \`/tictactoe\`

- M'inviter sur un autre serveur: \`/invite_link\`

__En étant admin:__

- Gérer les commandes customs: \`/command add\` et \`/command remove\`

- Gérer les logs du serveur: \`/log enable\` pour activer un type de log, et \`/log disable\` pour le désactiver
Pour voir quels logs sont activés: \`/log list\`
Il est aussi possible d'ignorer certains channels (pour ne pas qu'ils soient affichés dans les logs) avec \`/log ignore\`

- Gérer le rôle automatique: \`/autorole enable\` pour l'activer, et \`/autorole disable\` pour le désactiver
Pour savoir quel rôle automatique est activé: \`/autorole info\`
Activer un rôle automatique permet d'assigner ce rôle à tous les nouveaux arrivants d'un serveur

- Purger les messages d'un channel: \`/purge\``;
